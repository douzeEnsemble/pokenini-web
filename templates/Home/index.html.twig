{% set currentRouteParameters = app.request.attributes.get('_route_params') %}
{% set locale = app.request.locale %}

{% extends 'base.html.twig' %}
{% use '_nav.html.twig' %}
{% import "AlbumDexList/_macro.html.twig" as list %}

{% block title %}Pokénini{% endblock %}

{% block stylesheets %}
  {{ parent() }}

  <link rel="stylesheet" href="{{ asset('css/home.css') }}">
  <link rel="stylesheet" href="{{ asset('css/album-dex-list.css') }}">
{% endblock stylesheets %}

{% block container %}
<div id="home" class="row">
  <div class="col-lg-10 mx-auto text-center">
    <h1>Pokénini</h1>

    <div class="mt-3 mb-3 alert alert-warning" role="alert">
      <p>
        {{ 'home.alert.not_connected.description'|trans|htmlNl2br }}
      </p>
      <a href="{{ path('app_connect_index') }}" class="btn btn-warning">
        {{ 'home.alert.not_connected.action'|trans }}
      </a>
    </div>

    {{ list.list(dex, locale, demoUserId) }}
  </div>
</div>
{% endblock %}

{% block foot_javascript %}
  {{ parent() }}
  <script>
  (function() {
    // Enable tooltips
    [...document.querySelectorAll('[data-bs-toggle="tooltip"]')]
      .map(tooltipTriggerEl => new bootstrap.Tooltip(tooltipTriggerEl));
  })();
  </script>
{% endblock foot_javascript %}
