{% import "common/_progress_bar_macros.html.twig" as progressBar %}
<div id="election-actions-top" class="election-actions sticky-top">
  <div class="progress-container">
    {{ progressBar.roundBar(metrics.roundCount, metrics.totalRoundCount, metrics.completion.underMaxCount, metrics.completion.atMaxCount, pokemons|length, 'pick' == listType ? 'info' : 'warning') }}
  </div>
</div>
